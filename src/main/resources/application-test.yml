spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/starter_test
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: validate
  grpc:
    server:
      port: 0
  shell:
    interactive:
      enabled: false

standard-tools:
  market-data:
    default-provider: stub
    cache-ttl: 5m
    providers:
      yfinance:
        enabled: false
      polygon:
        enabled: false
      bloomberg:
        enabled: false

logging:
  level:
    org.testcontainers: INFO
    org.flywaydb: INFO
